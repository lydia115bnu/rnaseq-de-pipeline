// conf/base.config — sensible resource defaults for all environments

process {
    // Capture all stdout/stderr to log files
    // Each module declares its own log: block; this is a safety net
    shell = ['/bin/bash', '-euo', 'pipefail']
}
